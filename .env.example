# Porta do servidor (Railway/Vercel definem automaticamente)
PORT=5000

# Ambiente
NODE_ENV=production

# Banco de dados PostgreSQL
# Railway: Esta variável é criada automaticamente quando você adiciona PostgreSQL ao projeto
# Vercel: Configure através da integração com Neon ou outro provedor PostgreSQL
# Local: Use sua conexão local postgresql://usuario:senha@localhost:5432/nome_db
DATABASE_URL=postgresql://postgres:password@localhost:5432/gohan_timeline

# Vercel Blob Storage (somente para Vercel, não necessário no Railway)
BLOB_READ_WRITE_TOKEN=your_blob_token_here

# Variáveis individuais do PostgreSQL (Railway fornece automaticamente)
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=password
PGDATABASE=gohan_timeline
